<?xml version="1.0" encoding="utf-8"?>
<!-- Splash natif Android (pré-Android 12) — affiché avant le démarrage de
     Flutter. Uniquement un fond dégradé navy, SANS logo : le logo BABIFIX
     + le texte CLIENT s'animent une seule fois sur le splash Dart pour
     éviter tout doublon de logo. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@drawable/splash_gradient" />
</layer-list>
